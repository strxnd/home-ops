---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: minecraft
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
spec:
  endpoints:
    - dnsName: "mc.${SECRET_DOMAIN}"
      recordType: A
      targets:
        - "${SECRET_PUBLIC_IP}"
      providerSpecific:
        - name: "external-dns.alpha.kubernetes.io/cloudflare-proxied"
          value: "false"
    - dnsName: "_minecraft._tcp.mc.${SECRET_DOMAIN}"
      recordType: SRV
      targets:
        - "0 0 25565 mc.${SECRET_DOMAIN}"
